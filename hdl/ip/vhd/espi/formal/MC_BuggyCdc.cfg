\* TLC configuration: Buggy CDC (symmetric / bidirectional staleness).
\* EXTRA_LAG = 1 models a CDC implementation error where counters can be
\* stale in EITHER direction (not just the safe asymmetric direction).
\*
\* Expected: P1 (NoStaleData) FAILS — tx_rusedwds over-reports, response
\*           processor reads past end of FIFO, sends stale showahead data.
\*           P3 (NoPayloadUnderflow) FAILS — tx_rusedwds under-reports to 0,
\*           GET latches 0-length, payload_cnt underflows to 4095 in RTL.

SPECIFICATION SpecCompliant

CONSTANTS
    FIFO_DEPTH = 3
    MAX_MSG_SIZE = 2
    HOLD_THRESH = 2
    MAX_PAYLOAD = 2
    CDC_LAG = 0
    EXTRA_LAG = 1
    HDR_CYCLES = 1
    DataVal = {1, 2}

INVARIANT TypeOK
INVARIANT FifoDepthBound
INVARIANT NoStaleData
INVARIANT NoPayloadUnderflow
